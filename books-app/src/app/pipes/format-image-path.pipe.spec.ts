import { FormatImagePathPipe } from './format-image-path.pipe';

describe('FormatImagePathPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatImagePathPipe();
    expect(pipe).toBeTruthy();
  });
});
