import { SelectFileDirective } from './select-file.directive';

describe('SelectFileDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectFileDirective();
    expect(directive).toBeTruthy();
  });
});
